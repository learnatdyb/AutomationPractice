git status 

git init - for creating local repository [Only First Time]

git remote add origin "git url" - to establish connection between local and remote repository. [Only First Time]

git branch listners

git checkout listners


git add -A

git config --global user.name "smahendrancs" [Only First Time]

git config --global user.email "smahendrancs@gmail.com" [Only First Time]

git commit -m "adding listerners"

git push -u origin listners

git checkout master

git merge listeners

git push -u origin master

git branch -d listners

git push origin --delete listners